{
  "dependencies": {
    "axios": "^1.7.7"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0"
  },
  "scripts": {
    "start": "node server/index.js",
    "build": "cd client && npm install && npm run build"
  }
}

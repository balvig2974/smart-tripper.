{
  "dependencies": {
    "axios": "^1.7.7"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0"
  },
 "scripts": {
  "start": "npm run build && node server/express.cjs",
  "build": "cd my-app && npm install && npm run build"
}

}
